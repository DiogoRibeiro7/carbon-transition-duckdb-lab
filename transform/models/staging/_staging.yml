version: 2

models:
  - name: stg_owid_co2
    description: Typed staging view over the raw OWID CO2 CSV.
    columns:
      - name: country
        tests: [not_null]
      - name: year
        tests: [not_null]
  - name: stg_owid_energy
    description: Typed staging view over the raw OWID energy CSV.
    columns:
      - name: country
        tests: [not_null]
      - name: year
        tests: [not_null]
